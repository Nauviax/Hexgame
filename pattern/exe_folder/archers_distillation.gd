# Takes a position and a direction and returns the ENTITY hit by the raycast, or null if none is found.
# Direction (normalized vector) should be on top of stack
static var iota_count = 2
static func execute(hexlogic, _pattern):
	return "NOT_IMPLEMENTED_ERROR"