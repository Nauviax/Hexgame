# Takes a position and a direction and returns the SIZE of the block hit by the raycast, or null if none is found.
# If this hits the south face of a block, it would return [0, 1] (Because down is +y in Godot fuck me)
# Direction (normalized vector) should be on top of stack
static var iota_count = 2
static func execute(hexlogic, _pattern):
	return "NOT_IMPLEMENTED_ERROR"
